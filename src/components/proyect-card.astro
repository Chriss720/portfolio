---
const { title, image, description, url } = Astro.props;
---

<div class="flex flex-col bg-white border border-gray-200 rounded-xl shadow-sm hover:bg-gray-100 
            dark:border-gray-700 dark:bg-gray-800 dark:hover:bg-gray-700 w-full max-w-md overflow-hidden">
  
  <!-- Imagen -->
  <img 
    class="object-cover w-full h-64" 
    src={image} 
    alt={title} 
  />

  <!-- Contenido -->
  <div class="flex flex-col p-4">
    <h5 class="text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{title}</h5>
    <p class="mt-2 text-gray-700 dark:text-gray-400">{description}</p>
    <a href={url}
       target="_blank" 
       rel="noopener noreferrer"
       class="mt-3 text-sm text-blue-600 dark:text-blue-400 hover:underline">
      View project
    </a>
  </div>
</div>


